<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Информация о студенте</title>
    <!-- Подключение Google Fonts для современных шрифтов -->
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&family=Open+Sans&display=swap" rel="stylesheet">
    <style>
        /* Сброс некоторых стандартных стилей браузера */
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Roboto', sans-serif;
            background: linear-gradient(135deg, #6B73FF 10%, #000DFF 100%);
            display: flex;
            height: 100vh;
            justify-content: center;
            align-items: center;
            margin: 0;
            color: #fff;
        }

        .container {
            background-color: rgba(255, 255, 255, 0.1);
            padding: 40px 60px;
            border-radius: 15px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.2);
            text-align: center;
            backdrop-filter: blur(10px);
            max-width: 400px;
            width: 90%;
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .container:hover {
            transform: translateY(-10px);
            box-shadow: 0 25px 45px rgba(0, 0, 0, 0.3);
        }

        h1 {
            margin-bottom: 15px;
            font-family: 'Open Sans', sans-serif;
            font-size: 1.8em;
            color: #fff;
            text-shadow: 1px 1px 2px rgba(0,0,0,0.3);
        }

        p {
            margin-bottom: 25px;
            font-size: 1.1em;
            color: #e0e0e0;
        }

        .button {
            background: linear-gradient(45deg, #FF6B6B, #F06595);
            border: none;
            color: white;
            padding: 15px 30px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 1em;
            border-radius: 30px;
            cursor: pointer;
            transition: background 0.3s, transform 0.2s;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
        }

        .button:hover {
            background: linear-gradient(45deg, #F06595, #FF6B6B);
            transform: scale(1.05);
            box-shadow: 0 15px 25px rgba(0, 0, 0, 0.3);
        }

        /* Адаптивность для мобильных устройств */
        @media (max-width: 480px) {
            .container {
                padding: 30px 20px;
            }

            h1 {
                font-size: 1.5em;
            }

            p {
                font-size: 1em;
            }

            .button {
                width: 100%;
                padding: 12px 0;
                font-size: 0.9em;
            }
        }
    </style>
</head>
<body>

    <div class="container">
        <h1>Николай Прохоренко</h1>
        <p>Учебная группа: РСБО-01-23</p>
        <button class="button" onclick="openVKPage()">Перейти на страницу ВКонтакте</button>
    </div>

    <script>
        function openVKPage() {
            window.open('https://vk.com/daroyihz', '_blank');
        }
    </script>

</body>
</html>
